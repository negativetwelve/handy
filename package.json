{
  "name": "handy",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=4",
    "yarn": ">=0.27.5"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-jolt": "^2.4.0",
    "eslint": "^4.4.1",
    "eslint-config-jolt": "^2.4.0",
    "husky": "^0.14.3",
    "jest": "^20.0.4",
    "jest-preset-jolt": "^2.4.0",
    "lerna": "^2.0.0",
    "lint-staged": "^4.0.3",
    "rimraf": "^2.6.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "lint-staged": {
    "*.js": [
      "yarn lint"
    ]
  },
  "jest": {
    "preset": "jest-preset-jolt",
    "testEnvironment": "node"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "clean": "lerna clean --yes && lerna run clean",
    "lint": "eslint .",
    "precommit": "lint-staged",
    "test": "jest",
    "test:build": "yarn run build && yarn run test"
  }
}
