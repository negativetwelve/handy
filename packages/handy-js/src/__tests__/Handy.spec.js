// Modules
import Handy from '../Handy';


describe('Handy', () => {
  describe('Datetime', () => {
    it('should exist', () => {
      expect(Handy.Datetime).toBeDefined();
    });
  });

  describe('Filesize', () => {
    it('should exist', () => {
      expect(Handy.Filesize).toBeDefined();
    });
  });
});
